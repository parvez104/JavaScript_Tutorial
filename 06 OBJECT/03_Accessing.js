//🔴🟡🟢 Accsessing Object value : 

var point = {
    x: 10,
    y: 20,
    z: 40
}

// dot(.) notetion - 
console.log(point.x)
console.log(point.y)
console.log(point.y + point.z)

// ['value'] array notetion - 

console.log(point['x'])
console.log(point['y'])

var show = 'x' // x property value
console.log(point.show) //undefind 
console.log(point[show])

//Note : jokon amra janbo na amrader kon value niye kaj korte hobe, kon data ke explore korte hobe, variable ar maddme asbeh, dynamic vave asbeh, tokon amra array notetion use korbo, Otherways we use dot. notetion.

